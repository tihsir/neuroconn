subjects = None
modes = None

for sub_x, subject in enumerate(subjects):
    for mode_x, mode in enumerate(modes):

        read_dir = 'some path'
        write_dir = 'some other path'

        #Loading time courses file from drive
        courses = np. 
